-- conf.lua

function love.conf(t)
    t.window.title = "WASD Movement Game"  -- Set the window title
    t.window.width = 800  -- Set the window width
    t.window.height = 600  -- Set the window height
end
